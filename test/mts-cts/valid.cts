// Valid CommonJS TypeScript (.cts) file.
// Verifies that .cts files are parsed with type information (projectService)
// and pass the TypeScript rule set without crashing or false positives.

export const farewell = 'goodbye from a cts module'
