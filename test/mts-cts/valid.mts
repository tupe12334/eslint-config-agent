// Valid ESM TypeScript (.mts) file.
// Verifies that .mts files are parsed with type information (projectService)
// and pass the TypeScript rule set without crashing or false positives.

export const greeting = 'hello from an mts module'
