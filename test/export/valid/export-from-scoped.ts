// Valid: Export from scoped package (allowed) and relative paths

export { Component } from "@/components";
export { Utils } from "@myorg/utils";
export { Helper } from "./helper";
export { Config } from "../config";